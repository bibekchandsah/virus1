# Render Blueprint - https://render.com/docs/blueprint-spec
services:
  - type: web
    name: email-scraper
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
